const math = require('./math'); //Relative path
console.log(math.add(5, 7)); //Using the add function(math.subtract(10, 4)); //Using the subtract function
console.log(math.subtract(10, 3)); //Using the subtract function